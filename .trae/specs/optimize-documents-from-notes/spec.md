# 基于 6 份笔记 + xiaoyingxiong-novel-writer skill 的 2 份 sub-agent 文档 Spec 优化（v3 引导式）

## Why（背景）
经过阶段 4.1-4.3：
- 6 份 `pixiv_深度阅读笔记_*.md` 已完成抽样深度阅读（**样本**）
- `xiaoyingxiong-novel-writer` skill 已部署到 `/workspace/帝王战队621 1：00/skills/xiaoyingxiong_novel_writer/`（**框架**），内含：
  - SKILL.md（核心方法论 4-6 步 + 触发场景 + 输出规范 + 注意事项）
  - 6 个 references（character_archetypes.md / plot_templates.md / scene_hooks.md / dialogue_style.md / vocabulary.md / rhythm_templates.md）

但**sub-agent 启动时只接收 2 份文档**：
1. `sub-agent分配速查表.md`（762 行，仅做分配）
2. `sub-agent输入输出规范.md`（192 行，执行模式 + 灵活模板）

这 2 份文档**未指向已有资源**——sub-agent 不知道"还有 6 份样本笔记"和"还有 xiaoyingxiong skill 框架"可以参考。

**用户硬性要求**（多次反馈）：
- "**发给子代理就只有这两个文档**"——2 份文档是 sub-agent 启动时拿到的**全部资料**
- "**速查表与规范只引导子代理的阅读走向，不定死方向**"——**只引导，不定死**
- "**重新做规划**"——v1 / v2 spec 都已被驳回
- **新指令**："先解压在帝王战队 skills文件夹里的xiaoyingxiongskill压缩包，阅读一下再写优化"——**必须读 xiaoyingxiong skill 后再写 spec**

## What Changes（变更内容）

**只改 2 份文档**（不动 6 份笔记、不动 77 部源小说、不动 sub-agent 数量、不动 xiaoyingxiong skill）。

### A. `sub-agent分配速查表.md`（当前 762 行）
**保留**（一字不动）：
- 6 批量总览（8/27/24/4/6/4 = 73 sub-agent）+ 104 分配单元
- 每个 SA-XX-X 的作品 / 完整路径 / 章节 / KB / 写入段落
- 500KB 硬约束 ✓ 标注
- 大文件 offset/limit 分段指引
- 同名/相似名 易混淆提示
- 大文件读取提示（避免上下文溢出）
- 完成回报格式
- 实测边界说明

**新增**（末尾加 1 个"📖 可参考资源"小节，**30-50 行**）：
1. 列出 `xiaoyingxiong_novel_writer` skill 路径（**可参考框架**）
2. 列出 6 份深度阅读笔记路径（**可参考样本**）
3. 措辞**统一为"可参考/建议关注/推荐借鉴"**
4. **不写**"必含/必标/强制/严禁/不进入自动生成"
5. **不写**任何具体必含元素数量（如"必出现至少 X 个"）

**禁止添加**（之前犯的错）：
- 禁止"必出现至少 X 个"硬指标
- 禁止"必标"/"必含"/"强制减分"
- 禁止"4 级踩裆强度 L4 严禁"等强约束
- 禁止在每批量头部塞大段洞察
- 禁止烘焙 xiaoyingxiong skill 的具体内容到 2 份文档（sub-agent 可自行查阅 skill）

### B. `sub-agent输入输出规范.md`（当前 192 行）
**保留**（一字不动）：
- 方案 1（批次内并行 → 串行合并）
- 13 元素灵活模板
- 唯一硬性要求：v2 SKILL 升级建议 ≥ 1 条
- 关键章节 / 核心套路 / 关键场景 三选二覆盖
- Step 0-8 流程
- V1/V2/V3 验证
- 文档关系图
- 风险与缓解

**新增**（末尾加 1 个"📖 可参考资源"小节，**30-50 行，与速查表同步**）：
1. 列出 `xiaoyingxiong_novel_writer` skill 路径
2. 列出 6 份深度阅读笔记路径
3. 措辞**统一为"可参考/建议关注/推荐借鉴"**
4. **不写**任何硬约束

**禁止添加**（之前犯的错）：
- 禁止把 13 元素灵活模板改成硬模板
- 禁止加"修真玄幻雷点强制减分 2-3 分"
- 禁止加"修真/外语不进入自动生成"
- 禁止加"触手术语强制改写"
- 禁止加"高风险 IP frontmatter 强制标注"

## Impact（影响范围）
- **只改 2 份文档**：`/workspace/帝王战队621 1：00/documents/sub-agent分配速查表.md` + `sub-agent输入输出规范.md`
- **每份文档末尾加 1 个"📖 可参考资源"小节**（30-50 行），2 份文档内容同步
- **不动**：6 份深度阅读笔记、77 部源小说、73 sub-agent 分配、tmp/execution_packets.md、xiaoyingxiong skill
- **不新建**：不新增任何文件（除 spec/tasks/checklist）

## 与 xiaoyingxiong skill 的关系

**xiaoyingxiong-novel-writer skill 是 sub-agent 可选的参考框架**（sub-agent 是否能加载取决于其执行环境）。
- 13 元素灵活模板 与 xiaoyingxiong skill 的结构**部分对齐**（角色弧光 / 关键场景 / 核心套路 / 写作技法 等元素）
- 2 份文档**只**在末尾"📖 可参考资源"小节**点名**该 skill，**不**复述其内容
- sub-agent 自行决定是否加载、加载多少

**6 份深度阅读笔记是 sub-agent 可选的参考样本**：
- 2 份文档**只**在末尾"📖 可参考资源"小节**点名**6 份笔记，**不**复述其内容
- sub-agent 自行决定是否查看、如何参考

## 引导 vs 定死的关键差异（用户原话）

| ❌ 定死（之前犯的错） | ✅ 引导（v3 改进） |
|---|---|
| "sub-agent 笔记中必出现至少 5 个 T 公式" | "📖 可参考：xiaoyingxiong skill 的 plot_templates.md" |
| "必含元素 E1-E5 详细化" | "📖 可参考：6 份深度阅读笔记" |
| "修真玄幻雷点清单：强制减分 2-3 分" | "📖 可参考：6 份深度阅读笔记" |
| "触手术语改写'榨精魔物'是强制约束" | "📖 可参考：xiaoyingxiong skill 的 vocabulary.md" |
| "同人/女性向 10 高风险 IP frontmatter 强制标注" | "📖 可参考：6 份深度阅读笔记" |
| "修真/外语不进入自动生成" | "📖 可参考：6 份深度阅读笔记" |
| "5 大可复用技法层（02/04/06 借用）" | "📖 可参考：xiaoyingxiong skill 的 plot_templates.md" |

## 硬约束（用户要求）
1. **只改 2 份文档**：速查表 + 输入输出规范
2. **速查表保持"分配速查"本质**：每 SA-XX-X 的 KB / 章节 / 路径不变
3. **保持 500KB 硬约束 + 拆段指引**：用户已多次强调不许删
4. **保持 no 字符下限**：不锁字数
5. **保持 13 元素灵活模板**：不锁元素数
6. **保持 方案 1 执行模式**：不换方案
7. **保持 v2 SKILL 升级建议 ≥ 1 条**：唯一硬性要求
8. **新增内容措辞**：用"可参考/建议关注/推荐借鉴"，**不写"必含/必标/强制/严禁"**
9. **每份文档末尾只加 1 个小节**：📖 可参考资源
10. **2 份文档的"📖 可参考资源"小节内容保持一致**（同步）
11. **xiaoyingxiong skill 是可参考资源，sub-agent 自行决定是否加载**（不在 2 份文档中复述其内容）
12. **6 份深度阅读笔记是可参考样本，sub-agent 自行决定是否参考**（不在 2 份文档中复述其内容）
