# Standardize Pixiv Chapter Names — Checklist

- [x] 1. `pixiv_workspace/` 已解压；`作品信息.txt` 共 107 个、章节 `.txt` 共 1034 个已统计确认
- [x] 2. 脚本 `pixiv_renamer.py` 能在空跑（dry-run）模式下列出所有重命名计划
- [x] 3. 解析 作品信息 阶段：所有非空 作品信息 都能提取到 `标题` 与 `作品id`；空文件被记录
- [x] 4. `#N` 文件解析：所有以 `#N` 开头的章节文件都能进入映射
- [x] 5. `#N → 作品id` 默认对齐（`#1` 对应作品信息中第 1 条 = 最新）正确处理至少一个抽样作品
- [x] 6. 错字回退：抽样 `深渊之役`（含 `#2 第二张` 错字、`#6` 重复第二章）能正确归并到对应作品id
- [x] 7. 无 `#N` 前缀文件处理：抽样 `02_勇者魔物奇幻/奥鲁斯托/`、`《为什么被榨精的总是我！...》.txt` 等被识别为 `untouched` 或正确匹配
- [x] 8. 新文件名保留全部原标点（书名号、破折号、冒号、引号、全角空格等）
- [x] 9. 命名冲突自动加 `(2)` 后缀（用 `英雄游戏 上/下` 等案例验证）
- [x] 10. Windows 非法字符清洗：标题中若含 `<>:"/\|?*` 等不会被原样保留
- [x] 11. 输出目录 `/workspace/pixiv_renamed/` 完整创建，目录结构与 `pixiv_workspace/` 一致
- [x] 12. 每个输出文件的字节内容与源文件一致（无损坏）
- [x] 13. `作品信息.txt` 在 `pixiv_renamed/` 下原样保留
- [x] 14. 原 `/workspace/pixiv_workspace/` 未被修改
- [x] 15. `rename_report.md` 写入并包含：总览、每个作品的重命名表、需人工核对清单
- [x] 16. 抽样 3 个作品人工核对：抽样作品的每一条 `旧→新` 与预期一致
