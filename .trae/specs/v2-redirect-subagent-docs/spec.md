# Sub-agent 2 份文档 v2 反转 + 元素池扩展 + 大类引导方向（草稿态）

## Why（背景）

v3 spec（`optimize-documents-from-notes`）方向被用户**反转**，且我上一版 v2 spec 又把方向搞反一次：

- **v3 错方向**：把 6 份 `pixiv_深度阅读笔记_*.md` 和 xiaoyingxiong skill 当作"sub-agent 可参考资源"列在 2 份文档末尾"📖 可参考资源"小节
- **v2 上一版错方向**：撤了"📖 可参考资源"小节，但又把"sub-agent 主动读 references/ + 6 笔记"塞进 Step 0.5/6.5
- **v2 本版正方向**：
  - 撤掉"📖 可参考资源"小节，**同时不复活**任何形式的"sub-agent 读 references/ 或 6 笔记"工作流
  - 6 份笔记是**用户**读的，**不是** sub-agent 的输入
  - references/（xiaoyingxiong skill 6 个子文件）是**优化 skill 时**读的，**不是** sub-agent 的输入
  - **优化 skill 时只读 SKILL.md 一个文件**（不读 references/）
  - 但**可新增** references/ 子文件
  - sub-agent **只看本规范 + 速查表 + 小说本身**，输出"灵活模板笔记"
  - 笔记反向**优化 skill**（草稿做完后用户读 6 笔记 + 结合草稿再优化 skill）
- **原小说位置不变**：`/workspace/帝王战队621 1：00/pixiv小说`（用户原话："原小说位置也没改呀"）

第三节 13 元素池不够丰富（仅 13 项），需扩展到 **20+ 元素**；按 6 大类（6 批量）每类写 **3-5 个引导方向**——这两项**独立于"反转"**。

## What Changes（变更内容）

### A. 撤销 v3 "📖 可参考资源"小节（2 份源文档）
- **输入输出规范**：删除 L196-236 的 "## X+1. 📖 可参考资源（不进入必含清单...）"整段（41 行）
- **速查表**：删除 L766-806 的 "## X+5. 📖 可参考资源（不进入必含清单...）"整段（41 行）

### B. 删除速查表 X+1（X+1 → X+0）
- **速查表**：删除 L697-719 的 "## X+1. 大文件读取提示（避免上下文溢出）"整段（23 行）
- 重新编号：
  - 原 "## X+2. 完成回报格式" → **"## X+1. 完成回报格式"**
  - 原 "## X+3. 速查表 vs 源文档关系" → **"## X+2. 速查表 vs 源文档关系"**
  - 原 "## X+4. 实测边界说明（2026-06-20）" → **"## X+3. 实测边界说明（2026-06-20）"**

> 注：删除 X+1 后，500KB 硬约束在 X+3 实测边界说明中已**保留**（不丢失）。

### C. 第三节扩展 13 元素池到 20+ 元素
- **位置**：`/workspace/帝王战队621 1：00/documents/sub-agent输入输出规范.md` L61-77 的 "### 3.1 可选元素池（13 项，AI 自由选用）"
- **目标**：扩展到 **20-25 元素**
- **新增元素**（候选 12 个，最终选 7-12 个合并入池）：

| # | 新增元素 | 适用场景 | 自由发挥点 |
|---|---|---|---|
| 14 | **XP 命中标签** | 几乎所有 | 触手控 / 足控 / NTR / 群像 / 兄弟 / 调教 / 淫纹 / 催眠 / 收藏 / 跨服等 1-N 个标签 |
| 15 | **角色数据卡** | 几乎所有 | 身高 / 体重 / 年龄 / 肉棒 / 足码 / 体味 / 弱点 / 称号 |
| 16 | **沦陷阶段曲线** | 调教/淫纹/触手 | 5 阶段沦陷（傲娇→含蓄→决绝→软化→崩溃）标注 + 关键转折章 |
| 17 | **关系图谱** | 战队/双主角/纯爱 | 主角-配角-反派 的三角 / 多人关系、亲缘/师徒/兄弟/敌对 |
| 18 | **场景空间** | 全部 | 标志性地点（更衣室 / 保健室 / 公园 / 公交 / 训练场 / 异世界） |
| 19 | **调教师/反派分级** | 调教/触手/催眠 | 淫纹师 / 催眠博士 / 触手怪人 / 收藏家 / 流氓少年群 等 |
| 20 | **节奏与时长** | 长篇/精品 | 章节长度 / 场景切换频率 / 高潮位置 / 收束方式 |
| 21 | **专属术语/称号** | 全部 | 作品内特殊词汇（"贱屌应龙""英雄犬""小淫龙"） + 沦陷前后称呼变化 |
| 22 | **风险标记** | 全部 | IP frontmatter（斗罗同人/原神同人/海贼同人/宝可梦同人/超级战队） |
| 23 | **风格借鉴 vs 套路识别** | 全部 | 借鉴来源（日语轻小说/网文/翻译腔）+ 是否可复用 |
| 24 | **隐藏角色/配角弧光** | 长篇/精品 | 不只主角，配角的成长 / 黑化 / 救赎 |
| 25 | **跨类型桥** | 全部 | 与其他子类型的桥接可能（如 02 勇者 ↔ 01 战队） |

- **保留原 13 元素**：1-13 一字不动
- **唯一硬性要求保持**："v2 SKILL 升级建议 ≥ 1 条"（仍为唯一兜底）
- **三选二覆盖保持**："关键章节 / 核心套路 / 关键场景" 至少覆盖 2 项

### D. 第三节新增 "### 3.4 大类引导方向（6 大类 × 3-5 方向）"
- **位置**：插入到 L77 之后，L79 之前（在 3.2 指引原则之前）
- **6 大类**（按 6 批量 + pixiv 子类目）：

#### 3.4.1 01_战队特摄（8 部，xiaoyingxiong 主战场）
- 战队关系网（队员代号/色彩/坐骑/必杀技 + 队长-副队长-队员三角）
- 变身与被俘流程（变身器/口令 → 战败 → 战衣剥离 → 改造）
- 淫纹/印记植入路径（从"英雄"到"英雄犬"的身份降级）
- 收藏与展示（拍卖会 / 训练场 / 公共空间 / 直播）
- 反派等级梯度（淫纹师 / 触手怪人 / 催眠博士 / 收藏家 / 流氓少年群）

#### 3.4.2 02_勇者魔物奇幻（27 部）
- 异世界设定（穿越/原生/位面交叠）+ 地图/势力/种族
- 勇者小队关系（队长-副队-法师-盗贼-僧侣 + 卧底/内鬼）
- 魔物种类谱（触手/兽/人外/半人半魔 + 等级/特性/弱点）
- 任务/副本结构（主线/支线/隐藏 + 战败即沦陷）
- 魔法体系（元素/属性/技能树 + 队伍配合 / 个人觉醒）

#### 3.4.3 03_正太校园堕落（24 部）
- 校园空间（教室 / 更衣室 / 保健室 / 体育仓库 / 厕所 / 校车 / 放学后教室）
- 师生关系（老师-学生 / 教官-学员 / 教练-队长的非常规接触）
- 同学霸凌（群像堕落 / 单人胁迫 / 旁观者沉默）
- 社团组织（运动部 / 文化部 / 课外小组 / 同好会）
- 纯爱 vs 变态（甜文 vs 病娇 / 傲娇 vs 顺从 / 双主角 vs 群像）

#### 3.4.4 04_调教拍卖+05_异种触手（4 部）
- 拍卖流程（鉴定 → 标价 → 展示 → 竞价 → 交付 → 售后）
- 收藏家分级（业余/专业/企业/王室 + 收藏动机）
- 触手种类与功能（吸附 / 缠绕 / 注入 / 改造 / 触手交合）
- 非人反派（触手怪人 / 半人半兽 / 兽人 / 植物人 / 元素生命）
- 驯化过程（捕获 → 标记 → 改造 → 服从 → 主动配合）

#### 3.4.5 05_修真玄幻+06_外语（6 部）
- 修真境界体系（炼气→筑基→金丹→元婴→化神 + 雷劫/渡劫）
- 玄幻势力（宗门/世家/帝国/魔道/妖族 + 联盟/敌对/中立）
- 跨文化设定（外语原作的语言风格 + 翻译腔 + 文化梗）
- 外语味对白（日语/英语/俄语/法语 习惯用语 + 称呼体系）
- 异国地名（虚构 + 真实地名的改写规则）

#### 3.4.6 06_同人+07_女性向（4 部）
- 同人原 IP 标识（斗罗/原神/海贼/宝可梦/超级战队 + 角色映射）
- 女性向视角（甜文 / 虐心 / 治愈 / 致郁 + 性别倒错）
- 情感细腻度（心理独白比例 / 内心戏 / 眼神/微表情描写）
- 原作元素再诠释（角色关系重新搭配 / 新增角色 / 改动剧情线）
- 读者画像（女性向读者群 + 同人读者群的偏好差异）

### E. 撤掉"嵌入 sub-agent 主动读取"工作流（v2 上一版错方向，**删除**）
- **不**新增 Step 0.5"sub-agent 主动读取资源"
- **不**新增 Step 6.5"sub-agent 提交 skill 优化建议"
- **不**在 2 份文档任何位置出现"sub-agent 读 xiaoyingxiong references/"的字样
- **不**在 2 份文档任何位置出现"sub-agent 读 6 份深度阅读笔记"的字样
- sub-agent 工作流**仅**：
  1. 读速查表（确认分配）
  2. 读本规范（执行 + 灵活模板 + 风险）
  3. 读小说（按 SA-XX-X 指定路径）
  4. 写草稿
- 6 份笔记**不**作为 sub-agent 输入
- references/ **不**作为 sub-agent 输入

### F. skill 优化（草稿之后做，本 spec 范围外）
- 本 spec **只**做 A/B/C/D（清理 2 文档 + 扩展元素池 + 大类引导）
- skill 优化（改 SKILL.md / 新增 references/）**在草稿交付后**，用户阅读 6 笔记 + 结合草稿，再开新 spec
- 优化 skill 时**只读 SKILL.md 一个文件**（不读 references/），**可新增** references/ 文件
- 本 spec **不做** skill 任何文件改动

### G. 重新生成 12 份交付物
- 因为 2 份源文档变化（A/B/C/D），12 份交付物需重新生成
- **位置**：`/workspace/帝王战队621 1：00/documents/sub-agent-deliverables/`
- **改动点**：
  - 删除每份速查表 "## X+5" 段（已删除）
  - 删除每份速查表 "## X+1. 大文件读取提示" 段（已删除） + 重新编号 X+2/X+3/X+4 → X+1/X+2/X+3
  - 删除每份输入输出规范 "## X+1. 📖 可参考资源" 段（已删除）
  - 每份输入输出规范第三节同步扩展 13→20+ 元素 + 新增 3.4 大类引导方向
- **不**改动 12 份中的其他内容（不嵌入 Step 0.5/6.5）

## Impact（影响范围）

- **改 2 份源文档**：
  - `/workspace/帝王战队621 1：00/documents/sub-agent分配速查表.md`（806 → 742 行）
    - 删 X+1（23 行）+ 删 X+5（41 行）= -64 行
    - 重编号 X+2/X+3/X+4（标题 + 锚点）
  - `/workspace/帝王战队621 1：00/documents/sub-agent输入输出规范.md`（236 → ~270 行）
    - 删 X+1（41 行）
    - 扩展 13→20+ 元素池（+12 行）
    - 新增 3.4 大类引导方向（+30 行）
- **改 12 份交付物**：每份速查表 -X 行 + 重新编号；每份输入输出规范 同步扩展
- **不动**：
  - 6 份 `pixiv_深度阅读笔记_*.md`
  - 77 部源小说（位置 `/workspace/帝王战队621 1：00/pixiv小说`）
  - 73 sub-agent 分配（`tmp/execution_packets.md`）
  - xiaoyingxiong skill（任何文件）
  - 旧 spec 4 份（已完成）

## 与之前 spec 的关系

- **v3 spec（`optimize-documents-from-notes`）方向反转**：本 spec 是 v3 的"撤销 + 重新做"版本
- **v1 spec（`split-deliverables-by-batch`）保留**：12 份交付物结构不变，但内容同步
- **v0 spec（`optimize-subagent-allocation` / `simplify-assignment-and-plan`）保留**

## 硬约束（用户要求）

1. **撤删除**：删除 2 份文档"📖 可参考资源"小节（v3 加错的，v2 不要）
2. **删 X+1**：删除速查表"X+1. 大文件读取提示"段（用户原话）
3. **扩展元素池**：13 → 20+ 元素（用户原话）
4. **大类引导方向**：6 大类 × 3-5 个方向（用户原话）
5. **不嵌入 sub-agent 读 resources/ 或 6 笔记的工作流**：sub-agent 输入仅 = 速查表 + 本规范 + 小说
6. **唯一硬性要求保持**：v2 SKILL 升级建议 ≥ 1 条
7. **三选二覆盖保持**：关键章节 / 核心套路 / 关键场景 至少 2 项
8. **500KB 硬约束保留**：在 X+3 实测边界说明中（不丢失）
9. **no 字符下限**：不锁字数
10. **方案 1 执行模式**：不换方案
11. **优化 skill 时只读 SKILL.md 一个文件**（本 spec 不做 skill 优化）
12. **可新增 references 文件**（本 spec 不做）
13. **草稿之后再读 6 笔记 + 结合优化**（本 spec 不做）
14. **原小说位置不变**：`/workspace/帝王战队621 1：00/pixiv小说`

## 不做（明确范围）

- 不改 xiaoyingxiong skill（任何文件，包括 SKILL.md 和 references/）
- 不读 6 份 `pixiv_深度阅读笔记_*.md`（草稿之后再读）
- 不读 xiaoyingxiong skill 的 references/（**只读 SKILL.md** 优化 skill，但本 spec 不做）
- 不让 sub-agent 读 references/ 或 6 笔记（v2 上一版错方向）
- 不改 77 部源小说
- 不改原小说位置（`/workspace/帝王战队621 1：00/pixiv小说`）
- 不改 73 sub-agent 数量
- 不改 `tmp/execution_packets.md`
- 不写"📖 可参考资源"小节（已删，不复活）
- 不写"必含/必标/强制减分/严禁"等硬指标
- 不把 13 元素灵活模板改成硬模板（仅扩展元素数，不锁元素数）
- 不在 2 份文档中复述 6 笔记的具体洞察
- 不在 2 份文档中复述 xiaoyingxiong skill references/ 的具体内容
- 不在 2 份文档中复述 6 笔记 / references/ 的路径（彻底不出现）
