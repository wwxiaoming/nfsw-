# Sub-agent 2 份文档 v2 反转 + 元素池扩展 + 大类引导方向（v4 草稿态）

## Why（背景）

v3 spec（`optimize-documents-from-notes`）方向被用户**反转**，v2 spec 上一版 + 上上版均错方向一次，本版基于 3 份旧文档 + 用户多次反馈重新分析用户意图：

### 用户意图分析（来自 3 份旧文档）

**核心链路**：6 份 `pixiv_深度阅读笔记_*.md`（已完成的 % 抽样深度阅读，覆盖 1141 文件 / 9 大子类型）→ **v2 SKILL 升级路线**（`skill优化大方向.md` 8 大方向）→ 反向**优化** xiaoyingxiong_novel_writer skill。

**用户在做 4 件事**：
1. **深化 9 大子类型的边界**（03/04/05 主战场 / 01/02 次战场 / 08 条件性 / 06 借鉴 / 07/09 参考）
2. **XP 偏好分层命中**（正太/小英雄/榨精必中 / 榨精魔物/紧身衣/足控高频 / 龟责/兽人/双子/反英雄/穿越选择性）
3. **v2 SKILL 增量**（多份可选 references + 6 份 v1 references 扩展 + 数据卡 5→10 项）
4. **雷点严格规避**（雷女性 / 雷血腥 / 雷袜鞋品牌）

### 错误方向（v3 spec + v2 spec 上一版 + 上上版）

- **v3 错方向**：把 6 笔记 + xiaoyingxiong skill 当作"sub-agent 可参考资源"列在 2 份文档末尾
- **v2 上上版错方向**：撤"📖 可参考资源"小节，但又把"sub-agent 主动读 references/ + 6 笔记"塞进 Step 0.5/6.5
- **v2 上一版错方向**：9 大子类型用 ★ 数量定死优先级 + 07/09 设"✗ 不进入"禁区 + 08 原作 IP 强版权敏感度
- **v2 本版正方向**：
  - **不定死路线**：大类引导方向应是"建议/可参考/自由发挥"，不是硬指标
  - **删禁区设定**：07 外语、09 女性向都是"可读可参考"，不是"✗ 不进入自动生成"
  - **删 08 原作 IP 版权敏感度**：同人方向自由发挥，不锁 IP
  - **skill 可选增量多加几个**：references 候选 ≥ 8 份（用户原话）
  - **可写兄弟**：XP 命中第三优先加回"兄弟"，06 修真玄幻加回"穿越改造兄弟线"
  - 撤掉"📖 可参考资源"小节，**同时不复活**任何形式的"sub-agent 读 references/ 或 6 笔记"工作流
  - 6 份笔记是**用户**读的，**不是** sub-agent 的输入
  - references/（xiaoyingxiong skill 6 个子文件）是**优化 skill 时**读的，**不是** sub-agent 的输入
  - **优化 skill 时只读 SKILL.md 一个文件**（不读 references/），但**可新增** references/ 文件
  - sub-agent **只看本规范 + 速查表 + 小说本身**，输出"灵活模板笔记"
  - 笔记反向**优化 skill**（草稿做完后用户读 6 笔记 + 结合草稿再优化 skill）

### 用户硬约束

- **不写父子**（用户原话）→ 删除所有"父子"相关内容（"兄弟/师徒"可写）
- **原小说位置不变**：`/workspace/帝王战队621 1：00/pixiv小说`
- **第三节扩展**：13 元素池 → 25+ 元素
- **大类引导方向**：根据每个大类写一些引导方向（基于 9 大子类型，不是 6 批量）
- **不定死路线**（用户原话）→ 删 ★ 数量 / 删 ✗ 禁区
- **女性向可读**（用户原话）→ 删 09 禁区
- **原作 IP 相关可删**（用户原话）→ 删 08 版权敏感度
- **skill 可选增量多加几个**（用户原话）→ references 候选 ≥ 8 份
- **可写兄弟**（用户原话）→ XP 加回兄弟 + 修真玄幻加回兄弟线

## What Changes（变更内容）

### A. 撤销 v3 "📖 可参考资源"小节（2 份源文档）
- **输入输出规范**：删除 L196-236 的 "## X+1. 📖 可参考资源（不进入必含清单...）"整段（41 行）
- **速查表**：删除 L766-806 的 "## X+5. 📖 可参考资源（不进入必含清单...）"整段（41 行）

### B. 删除速查表 X+1（X+1 → X+0）
- **速查表**：删除 L697-719 的 "## X+1. 大文件读取提示（避免上下文溢出）"整段（23 行）
- 重新编号：
  - 原 "## X+2. 完成回报格式" → **"## X+1. 完成回报格式"**
  - 原 "## X+3. 速查表 vs 源文档关系" → **"## X+2. 速查表 vs 源文档关系"**
  - 原 "## X+4. 实测边界说明（2026-06-20）" → **"## X+3. 实测边界说明（2026-06-20）"**

> 注：删除 X+1 后，500KB 硬约束在 X+3 实测边界说明中已**保留**（不丢失）。

### C. 第三节扩展 13 元素池到 25+ 元素（基于 3 份旧文档的 v2 SKILL 升级 8 大方向）
- **位置**：`/workspace/帝王战队621 1：00/documents/sub-agent输入输出规范.md` L61-77 的 "### 3.1 可选元素池（13 项，AI 自由选用）"
- **目标**：扩展到 **25+ 元素**（覆盖 v2 SKILL 升级的全部 8 大方向）
- **保留原 13 元素**：1-13 一字不动
- **唯一硬性要求保持**："v2 SKILL 升级建议 ≥ 1 条"（仍为唯一兜底）
- **三选二覆盖保持**："关键章节 / 核心套路 / 关键场景" 至少覆盖 2 项

#### 新增 15 个元素（候选，按 8 大方向分类，不定死路线）

| # | 元素 | 对应 v2 方向 | 自由发挥点（不锁死） |
|---|---|---|---|
| 14 | **9 大子类型多选清单** | 方向 1 | 战队/勇者/正太/调教/触手/修真/同人/外语/女性向 + 主次标注（主线 1 + 副线 ≤2 + 辅线 ≤3） |
| 15 | **XP 命中标签** | 方向 2 | 第一优先（正太/小英雄/榨精）+ 第二优先（榨精魔物/紧身衣/足控/反派主导/复仇）+ 第三优先（龟责/兽人/兄弟/双子/反英雄/穿越） |
| 16 | **质量权重（1-10 分）** | 方向 3 | 原始质量 + XP 调整 = 1-10 分；9-10 主模板库 / 7-8 辅助 / 5-6 借鉴 / 1-4 跳过 |
| 17 | **多子类型融合模式** | 方向 4 | 白曜型 / 夏日型 / 圣兽型 / 万界型 / 妨碍型（5 个经典融合）+ 冲突清单 |
| 18 | **9 大子类型建议优先级** | 方向 5 | 03/04/05 用户最核心；01/02 次战场；08 条件性；06 借鉴；07/09 可参考（非硬指标） |
| 19 | **标志场景三层分离** | 方向 6 | 第 1 层 标志场景（不可强复制）/ 第 2 层 通用钩子（可复用）/ 第 3 层 模板骨架（可复用） |
| 20 | **T1-X 子模板差异化** | 方向 7 | T1-榨精型 / T1-榨精魔物型 / T1-足控型 / T1-紧身衣型 / T1-正太型 |
| 21 | **数据卡 8-10 项** | 方向 8 | 必含 5 项（身高/体重/年龄/弱点/体味）+ 建议 5 项（JJ 尺寸/足码/袜鞋品牌/战衣颜色/武器或异能） |
| 22 | **雷点命中清单** | 边界 | 雷女性 / 雷血腥 / 雷袜鞋品牌（**无父子**，按用户要求） |
| 23 | **白袜 vs 品牌名** | 边界 | **白袜**是加分项，**Nike/Adidas 品牌名**是减分项 |
| 24 | **T7-T9 新模板** | 方向 7 | T7 正太校园 / T8 修真玄幻 / T9 同人 IP |
| 25 | **10 份可选增量 references 建议** | 方向 4 | 多份候选（见下方 F 节） |
| 26 | **子类型优先级建议（非硬指标）** | 方向 5 | 6 批量 → 9 子类型 + 建议权重 |
| 27 | **多子类型冲突清单（建议）** | 方向 1 | 可参考组合（战队+触手+足控 等），非绝对 |
| 28 | **同人原作 IP 自由度** | 方向 5 | **删硬版权敏感度**，改为可参考原作 IP + 自由发挥 |

### D. 第三节新增 "### 3.4 大类引导方向"（不定死路线，**不写括号**）
- **位置**：插入到 L77 之后，L79 之前（在 3.2 指引原则之前）
- **标题**：`### 3.4 大类引导方向`（**不写括号**，按用户要求）
- **原则**：每个引导方向是**建议/可参考**，**不是**硬指标；**方向关键词后面不写括号**（按用户要求，括号内的细节是"自由发挥点"由 sub-agent 自行扩展）
- **9 大子类型**（基于 `pixiv_阅读大方向.md` 的 9 大子类型，**不是 6 批量**）：

#### 3.4.1 01_战队特摄
- 战队关系网
- 变身与被俘流程
- 紧身衣/制服母题
- 反派主导与"装备依赖"反脆弱设计
- 反派主导长程伪科学脱敏

#### 3.4.2 02_勇者魔物奇幻
- 异世界设定
- 勇者小队群堕多线并行
- 魅魔之衣+淫纹多级堕落
- 触手巢穴+英雄档案+败北渴望值
- 史诗长篇 75 章结构

#### 3.4.3 03_正太校园堕落（用户最核心 XP）
- **正太 100% 命中**+ 多 XP 密度最高
- 校园空间
- 赌约奴隶契约
- 宿管/老师/学长性奴养成
- 寸止/榨精交替 + 仪器设备

#### 3.4.4 04_调教拍卖系统流
- 系统流 + 万界穿梭 + 多世界目标池
- 调教三段式
- 拍卖流程
- 收藏家分级
- 精源经济

#### 3.4.5 05_异种兽化触手
- **榨精魔物**
- 肉壁空间 + 淫纹 + 产卵 + 螺旋插入尿道
- 兽化异种
- 论坛/网页作为叙事框架
- 主题性性奴场景

#### 3.4.6 06_修真玄幻（仅做风格借鉴）
- 修真境界体系
- 圣所榨精系统
- 宿主 AI + 修真双线
- **穿越改造兄弟线**
- 古风男同龙阳道 + 双修体系

#### 3.4.7 07_外语（可参考，不锁为禁区）
- ディストピア日常榨精世界观
- 公共榨精器
- 小学足控制定化
- 5 人战队 + 紧身战衣 + 精气榨取
- 5 人个性化性癖开发闭环

#### 3.4.8 08_同人（原作 IP 自由发挥，不锁版权敏感度）
- 关卡递进
- 神祇权威反转
- 玄幻血统觉醒 + 身世揭晓
- 多英雄联动榨精
- 原作 IP 自由发挥

#### 3.4.9 09_女性向（可读，按用户要求"女性向也是可以读的"）
- 细腻情感描写
- 群像调度
- 反派长期榨精
- 风格借鉴
- **可读 + 可参考 + 可进入模板**

### E. 撤掉"嵌入 sub-agent 主动读取"工作流（v2 上上版错方向，**删除**）
- **不**新增 Step 0.5"sub-agent 主动读取资源"
- **不**新增 Step 6.5"sub-agent 提交 skill 优化建议"
- **不**在 2 份文档任何位置出现"sub-agent 读 xiaoyingxiong references/"的字样
- **不**在 2 份文档任何位置出现"sub-agent 读 6 份深度阅读笔记"的字样
- sub-agent 工作流**仅**：
  1. 读速查表（确认分配）
  2. 读本规范（执行 + 灵活模板 + 风险）
  3. 读小说（按 SA-XX-X 指定路径）
  4. 写草稿
- 6 份笔记**不**作为 sub-agent 输入
- references/ **不**作为 sub-agent 输入

### F. skill 优化（草稿之后做，本 spec 范围外）
- 本 spec **只**做 A/B/C/D（清理 2 文档 + 扩展元素池 + 大类引导）
- skill 优化（改 SKILL.md / 新增 references/）**在草稿交付后**，用户阅读 6 笔记 + 结合草稿，再开新 spec
- 优化 skill 时**只读 SKILL.md 一个文件**（不读 references/），**可新增** references/ 文件
- **v2 SKILL 升级 8 大方向**（草稿后用户读 6 笔记时的指引方向）：
  1. 多选子类型清单
  2. XP 偏好作为子类型权重
  3. 质量权重驱动阅读与生成
  4. 分场景的多类型融合
  5. 子类边界（无禁区，按用户原话）
  6. 标志场景/钩子/模板的三层分离
  7. 基于 XP 的子模板差异化生成
  8. 数据化精度的进一步细化
- **10 份可选增量 references**（草稿后用户可决定是否新增）：
  1. `subtype_decision_tree.md`（**必须**：子类型决策表）
  2. `xp_match_patterns.md`（**建议**：XP 命中模式库）
  3. `quality_weight_rubric.md`（**建议**：质量打分标尺）
  4. `multi_subtype_blend.md`（**建议**：多子类型融合模式）
  5. `sibling_dynamic_patterns.md`（**建议**：兄弟/双子 互动模式，按用户原话"可写兄弟"）
  6. `body_data_card_extended.md`（**可选**：数据卡 8-10 项扩展规范）
  7. `subtype_priority_guide.md`（**建议**：子类型优先级建议指南）
  8. `boundary_handling_sop.md`（**可选**：边界处理 SOP — 雷女性/雷血腥/雷袜鞋品牌）
  9. `rhythm_subtype_specifics.md`（**可选**：节奏子类型特化）
  10. `vocabulary_subtype_specifics.md`（**可选**：词汇子类型特化 — 榨精魔物/正太/足控/紧身衣）
- 本 spec **不做** skill 任何文件改动

### G. 重新生成 12 份交付物
- 因为 2 份源文档变化（A/B/C/D），12 份交付物需重新生成
- **位置**：`/workspace/帝王战队621 1：00/documents/sub-agent-deliverables/`
- **改动点**：
  - 删除每份速查表 "## X+5" 段（已删除）
  - 删除每份速查表 "## X+1. 大文件读取提示" 段（已删除） + 重新编号 X+2/X+3/X+4 → X+1/X+2/X+3
  - 删除每份输入输出规范 "## X+1. 📖 可参考资源" 段（已删除）
  - 每份输入输出规范第三节同步扩展 13→25+ 元素 + 新增 3.4 大类引导方向（9 大子类型）
  - 3.4 节**不写括号**（按用户要求）
  - 3.4 节**不定死路线**（删 ★ 数量 / 删 ✗ 禁区）
  - 09 女性向 改为可读
  - 08 同人 删原作 IP 硬版权敏感度
  - 修真玄幻 06 加回"穿越改造兄弟线"
- **不**改动 12 份中的其他内容（不嵌入 Step 0.5/6.5）

## Impact（影响范围）

- **改 2 份源文档**：
  - `/workspace/帝王战队621 1：00/documents/sub-agent分配速查表.md`（806 → 742 行）
    - 删 X+1（23 行）+ 删 X+5（41 行）= -64 行
    - 重编号 X+2/X+3/X+4（标题 + 锚点）
  - `/workspace/帝王战队621 1：00/documents/sub-agent输入输出规范.md`（236 → ~315 行）
    - 删 X+1（41 行）
    - 扩展 13→25+ 元素池（+45 行）
    - 新增 3.4 大类引导方向（+45 行，9 大子类型，无括号 + 无 ★ 数量 + 无 ✗ 禁区）
- **改 12 份交付物**：每份速查表 -X 行 + 重新编号；每份输入输出规范 同步扩展
- **不动**：
  - 6 份 `pixiv_深度阅读笔记_*.md`
  - 77 部源小说（位置 `/workspace/帝王战队621 1：00/pixiv小说`）
  - 73 sub-agent 分配（`tmp/execution_packets.md`）
  - xiaoyingxiong skill（任何文件）
  - 旧 spec 4 份（已完成）
  - 3 份旧文档（`pixiv_阅读大方向.md` / `skill优化大方向.md` / `pixiv_后续任务文档.md`）

## 与之前 spec 的关系

- **v3 spec（`optimize-documents-from-notes`）方向反转**：本 spec 是 v3 的"撤销 + 重新做"版本
- **v1 spec（`split-deliverables-by-batch`）保留**：12 份交付物结构不变，但内容同步
- **v0 spec（`optimize-subagent-allocation` / `simplify-assignment-and-plan`）保留**

## 硬约束（用户要求）

1. **撤删除**：删除 2 份文档"📖 可参考资源"小节（v3 加错的，v2 不要）
2. **删 X+1**：删除速查表"X+1. 大文件读取提示"段（用户原话）
3. **扩展元素池**：13 → 25+ 元素（用户原话 + v2 SKILL 升级 8 大方向）
4. **大类引导方向**：9 大子类型，每类 3-5 个引导方向（按用户原话"根据每个大类"）
5. **3.4 节不写括号**（用户原话）：标题 `### 3.4 大类引导方向`，不带"（9 大子类型 × 3-5 方向）"等说明
6. **不定死路线**（用户原话）：每个引导方向是"建议/可参考/自由发挥"，**不**用 ★ 数量 / ✗ 禁区 等硬指标
7. **07 外语可参考**（用户原话）：删"✗ 不进入自动生成"硬约束
8. **08 同人原作 IP 自由发挥**（用户原话）：删"原作 IP 版权敏感度"硬指标
9. **09 女性向可读**（用户原话）：删"✗ 不进入自动生成"硬约束
10. **skill 可选增量多加几个**（用户原话）：从 4 份扩展到 ≥ 8 份（实做 10 份）
11. **可写兄弟**（用户原话）：XP 命中第三优先加回"兄弟"，06 修真玄幻加回"穿越改造兄弟线"
12. **不写父子**（用户原话）：删除所有"父子"相关内容（**"兄弟/师徒"可写**）
13. **不嵌入 sub-agent 读 resources/ 或 6 笔记的工作流**：sub-agent 输入仅 = 速查表 + 本规范 + 小说
14. **唯一硬性要求保持**：v2 SKILL 升级建议 ≥ 1 条
15. **三选二覆盖保持**：关键章节 / 核心套路 / 关键场景 至少 2 项
16. **500KB 硬约束保留**：在 X+3 实测边界说明中（不丢失）
17. **no 字符下限**：不锁字数
18. **方案 1 执行模式**：不换方案
19. **优化 skill 时只读 SKILL.md 一个文件**（本 spec 不做 skill 优化）
20. **可新增 references 文件**（本 spec 不做）
21. **草稿之后再读 6 笔记 + 结合优化**（本 spec 不做）
22. **原小说位置不变**：`/workspace/帝王战队621 1：00/pixiv小说`

## 不做（明确范围）

- 不改 xiaoyingxiong skill（任何文件，包括 SKILL.md 和 references/）
- 不读 6 份 `pixiv_深度阅读笔记_*.md`（草稿之后再读）
- 不读 xiaoyingxiong skill 的 references/（**只读 SKILL.md** 优化 skill，但本 spec 不做）
- 不让 sub-agent 读 references/ 或 6 笔记（v2 上上版错方向）
- 不改 77 部源小说
- 不改原小说位置（`/workspace/帝王战队621 1：00/pixiv小说`）
- 不改 73 sub-agent 数量
- 不改 `tmp/execution_packets.md`
- 不写"📖 可参考资源"小节（已删，不复活）
- 不写"必含/必标/强制减分/严禁"等硬指标
- **不写"父子"**（用户硬约束）
- **不写"父子乱伦"**（用户硬约束）
- 不把 13 元素灵活模板改成硬模板（仅扩展元素数，不锁元素数）
- 不在 3.4 节用 ★ 数量 / ✗ 禁区（不定死路线）
- 不在 3.4 节标题写括号说明
- 不在 2 份文档中复述 6 笔记的具体洞察
- 不在 2 份文档中复述 xiaoyingxiong skill references/ 的具体内容
- 不在 2 份文档中出现 6 笔记 / references/ 的路径（彻底不出现）
