# Sub-agent 2 份文档 v2 反转 + 元素池扩展 + 大类引导方向（v3 草稿态）

## Why（背景）

v3 spec（`optimize-documents-from-notes`）方向被用户**反转**，且 v2 spec 上一版又错方向一次，本版基于 3 份旧文档重新分析用户意图：

### 用户意图分析（来自 3 份旧文档）

**核心链路**：6 份 `pixiv_深度阅读笔记_*.md`（已完成的 % 抽样深度阅读，覆盖 1141 文件 / 9 大子类型）→ **v2 SKILL 升级路线**（`skill优化大方向.md` 8 大方向）→ 反向**优化** xiaoyingxiong_novel_writer skill。

**用户在做 4 件事**：
1. **深化 9 大子类型的边界**（03/04/05 主战场 / 01/02 次战场 / 08 条件性 / 06 借鉴 / 07/09 禁区）
2. **XP 偏好分层命中**（正太/小英雄/榨精必中 / 榨精魔物/紧身衣/足控高频 / 龟责/兽人/双子/反英雄/穿越选择性）
3. **v2 SKILL 增量**（4 份可选 references + 6 份 v1 references 扩展 + 数据卡 5→10 项）
4. **雷点严格规避**（雷女性 / 雷血腥 / 雷袜鞋品牌）

### 错误方向（v3 spec + v2 spec 上一版）

- **v3 错方向**：把 6 笔记 + xiaoyingxiong skill 当作"sub-agent 可参考资源"列在 2 份文档末尾
- **v2 上一版错方向**：撤"📖 可参考资源"小节，但又把"sub-agent 主动读 references/ + 6 笔记"塞进 Step 0.5/6.5
- **v2 本版正方向**：
  - 撤掉"📖 可参考资源"小节，**同时不复活**任何形式的"sub-agent 读 references/ 或 6 笔记"工作流
  - 6 份笔记是**用户**读的，**不是** sub-agent 的输入
  - references/（xiaoyingxiong skill 6 个子文件）是**优化 skill 时**读的，**不是** sub-agent 的输入
  - **优化 skill 时只读 SKILL.md 一个文件**（不读 references/），但**可新增** references/ 文件
  - sub-agent **只看本规范 + 速查表 + 小说本身**，输出"灵活模板笔记"
  - 笔记反向**优化 skill**（草稿做完后用户读 6 笔记 + 结合草稿再优化 skill）

### 用户硬约束

- **不要写父子**（用户原话）→ 删除所有"父子"相关内容
- **原小说位置不变**：`/workspace/帝王战队621 1：00/pixiv小说`
- **第三节扩展**：13 元素池 → 20+ 元素
- **大类引导方向**：根据每个大类写一些引导方向（基于 9 大子类型，不是 6 批量）

## What Changes（变更内容）

### A. 撤销 v3 "📖 可参考资源"小节（2 份源文档）
- **输入输出规范**：删除 L196-236 的 "## X+1. 📖 可参考资源（不进入必含清单...）"整段（41 行）
- **速查表**：删除 L766-806 的 "## X+5. 📖 可参考资源（不进入必含清单...）"整段（41 行）

### B. 删除速查表 X+1（X+1 → X+0）
- **速查表**：删除 L697-719 的 "## X+1. 大文件读取提示（避免上下文溢出）"整段（23 行）
- 重新编号：
  - 原 "## X+2. 完成回报格式" → **"## X+1. 完成回报格式"**
  - 原 "## X+3. 速查表 vs 源文档关系" → **"## X+2. 速查表 vs 源文档关系"**
  - 原 "## X+4. 实测边界说明（2026-06-20）" → **"## X+3. 实测边界说明（2026-06-20）"**

> 注：删除 X+1 后，500KB 硬约束在 X+3 实测边界说明中已**保留**（不丢失）。

### C. 第三节扩展 13 元素池到 25+ 元素（基于 3 份旧文档的 v2 SKILL 升级 8 大方向）
- **位置**：`/workspace/帝王战队621 1：00/documents/sub-agent输入输出规范.md` L61-77 的 "### 3.1 可选元素池（13 项，AI 自由选用）"
- **目标**：扩展到 **25+ 元素**（覆盖 v2 SKILL 升级的全部 8 大方向）
- **保留原 13 元素**：1-13 一字不动
- **唯一硬性要求保持**："v2 SKILL 升级建议 ≥ 1 条"（仍为唯一兜底）
- **三选二覆盖保持**："关键章节 / 核心套路 / 关键场景" 至少覆盖 2 项

#### 新增 15 个元素（候选，按 8 大方向分类）

| # | 元素 | 对应 v2 方向 | 自由发挥点 |
|---|---|---|---|
| 14 | **9 大子类型多选清单** | 方向 1 | 战队/勇者/正太/调教/触手/修真/同人/外语/女性向 + 主次标注（主线 1 + 副线 ≤2 + 辅线 ≤3） |
| 15 | **XP 命中标签** | 方向 2 | 第一优先（正太/小英雄/榨精）+ 第二优先（榨精魔物/紧身衣/足控/反派主导/复仇）+ 第三优先（龟责/兽人/双子/反英雄/穿越） |
| 16 | **质量权重（1-10 分）** | 方向 3 | 原始质量 + XP 调整 = 1-10 分；9-10 主模板库 / 7-8 辅助 / 5-6 借鉴 / 1-4 跳过 |
| 17 | **多子类型融合模式** | 方向 4 | 白曜型 / 夏日型 / 圣兽型 / 万界型 / 妨碍型（5 个经典融合）+ 冲突清单 |
| 18 | **9 大子类型边界** | 方向 5 | 03/04/05 ★★★★★；01/02 ★★★★；08 ★★★；06 ★★；07/09 ✗ |
| 19 | **标志场景三层分离** | 方向 6 | 第 1 层 标志场景（不可强复制）/ 第 2 层 通用钩子（可复用）/ 第 3 层 模板骨架（可复用） |
| 20 | **T1-X 子模板差异化** | 方向 7 | T1-榨精型 / T1-榨精魔物型 / T1-足控型 / T1-紧身衣型 / T1-正太型 |
| 21 | **数据卡 8-10 项** | 方向 8 | 必含 5 项（身高/体重/年龄/弱点/体味）+ 建议 5 项（JJ 尺寸/足码/袜鞋品牌/战衣颜色/武器或异能） |
| 22 | **雷点命中清单** | 边界 | 雷女性 / 雷血腥 / 雷袜鞋品牌（**无父子**，按用户要求） |
| 23 | **白袜 vs 品牌名** | 边界 | **白袜**是加分项，**Nike/Adidas 品牌名**是减分项 |
| 24 | **T7-T9 新模板** | 方向 7 | T7 正太校园 / T8 修真玄幻 / T9 同人 IP |
| 25 | **4 份可选增量 references 建议** | 方向 4 | subtype_decision_tree / xp_match_patterns / quality_weight_rubric / multi_subtype_blend |
| 26 | **子类优先级显式分级** | 方向 5 | 6 批量 → 9 子类型 + ★ 数量 |
| 27 | **多子类型冲突清单** | 方向 1 | 应避免组合（战队+修真+百合 / 触手+足控+治愈 / 同人+原创世界观 等） |
| 28 | **同人原作 IP 版权敏感度** | 方向 5 | 原神/斗罗/洛克人/漫威/暴雪/北欧神话/守望先锋/植物大战僵尸/英雄联盟/光环 → 显式提示用户版权风险 |

### D. 第三节新增 "### 3.4 大类引导方向（9 大子类型 × 3-5 方向）"
- **位置**：插入到 L77 之后，L79 之前（在 3.2 指引原则之前）
- **9 大子类型**（基于 `pixiv_阅读大方向.md` 的 9 大子类型，**不是 6 批量**）：

#### 3.4.1 01_战队特摄（★★★★ 107 文件 / 8 子目录 / 8 部 7-10 分）
- 战队关系网（队员代号/色彩/坐骑/必杀技 + 队长-副队长-队员三角）
- 变身与被俘流程（变身器/口令 → 战败 → 战衣剥离 → 改造）
- 紧身衣/制服母题（紧身战衣 / 薄膜战衣 / 乳胶衣 / 战斗服）
- 反派主导与"装备依赖"反脆弱设计（蜃煌"装备摘下=失去反抗力"）
- 反派主导长程伪科学脱敏（白曜"脱敏注射 + 项圈训狗"）

#### 3.4.2 02_勇者魔物奇幻（★★★★ 422 文件 / 32 子目录 / 19 部 7-10 分，**最大子类**）
- 异世界设定（穿越/原生/位面交叠）+ 地图/势力/种族
- 勇者小队群堕多线并行（队长-副队-法师-盗贼-僧侣 + 卧底/内鬼）
- 魅魔之衣+淫纹多级堕落（"修改—自愿—发心"三层）
- 触手巢穴+英雄档案+败北渴望值（系统流框架）
- 史诗长篇 75 章结构（v1 T1 模板的"史诗档"补全）

#### 3.4.3 03_正太校园堕落（★★★★★ 233 文件 / 29 子目录 / 20 部 7-10 分，**用户最核心 XP**）
- **正太 100% 命中**（25/25 部）+ 多 XP 密度最高（足控/榨精/榨精魔物/紧身衣都密集命中）
- 校园空间（教室/更衣室/保健室/体育仓库/厕所/校车/放学后）
- 赌约奴隶契约（暑假赌约 → 一个月契约 → 公开 play）
- 宿管/老师/学长性奴养成
- 寸止/榨精交替 + 仪器设备

#### 3.4.4 04_调教拍卖系统流（★★★★★ 85 文件 / 9 子目录 / 8 部 9-10 分）
- 系统流 + 万界穿梭（修仙/魔法/未来）+ 多世界目标池
- 调教三段式（准备篇 → 调教篇 → 间章启程）
- 拍卖流程（鉴定 → 标价 → 展示 → 竞价 → 交付 → 售后）
- 收藏家分级（业余/专业/企业/王室 + 收藏动机）
- 精源经济（精牛/精畜/挤奶世界 + 精源农场 + 修为返还系统）

#### 3.4.5 05_异种兽化触手（★★★★★ 83 文件 / 10 子目录 / 8 部 9-10 分，**100% 榨精魔物命中**）
- **榨精魔物**（替代触手术语，按用户原话）
- 肉壁空间 + 淫纹 + 产卵 + 榨精魔物螺旋插入尿道
- 兽化异种（兽城/九城多卷 + 多星域 + 兽人/触手兽/魅魔/黏液生物）
- 论坛/网页作为叙事框架（1 性癖 = 1 短篇）
- 主题性性奴场景（花丛/厨房/擂台/牢笼）

#### 3.4.6 06_修真玄幻（★★ 38 文件 / 5 子目录 / 4 部 5-10 分，**仅做风格借鉴**）
- 修真境界体系（炼气→筑基→金丹→元婴→化神 + 雷劫/渡劫）
- 圣所榨精系统（圣所+圣器+精液转灵气 + 属性化精液灵气）
- 宿主 AI + 修真双线（蓝蔚羽 + AI 诺亚 + 圣灵战队）
- 穿越置换修真位面 + 系统任务 + 修为返还体系（无伦常元素）
- 古风男同龙阳道 + 双修体系（**主角双男主**，非伦常）

#### 3.4.7 07_外语（✗ 65 文件 / 5 子目录，**不进入自动生成**，仅识别）
- ディストピア日常榨精世界观（"射精如同排泄般日常化"）
- 公共榨精器（购物中心等公共男子厕所常备）
- 小学足控制定化
- 5 人战队 + 紧身战衣 + エナジー（精气）榨取
- 5 人个性化性癖开发闭环

#### 3.4.8 08_同人（★★★ 77 文件 / 12 子目录 / 10 部 7-10 分，**视原作 IP 版权敏感度**）
- **原作 IP 版权敏感度**（原神/斗罗/洛克人/漫威/暴雪/北欧神话/守望先锋/植物大战僵尸/英雄联盟/光环）
- 关卡递进（旅馆→警长→精灵族→圣域中心→表弟）
- 神祇权威反转（蜜酒 + 血盟 + 言灵三重扭曲）
- 玄幻血统觉醒 + 身世揭晓（斗罗大陆同人）
- 多英雄联动榨精（英雄联盟 Ezreal 同人）

#### 3.4.9 09_女性向（✗ 31 文件 / 3 子目录，**不进入自动生成**，禁区）
- **不读内容**（按用户雷女性硬约束）
- **不进入 Skill 自动生成管线**
- 风格可参考（细腻情感描写、群像调度、权力跪姿、反派长期榨精）
- 但**不进生成模板**
- 仅在 v2 SKILL 中**显式标注边界**

### E. 撤掉"嵌入 sub-agent 主动读取"工作流（v2 上一版错方向，**删除**）
- **不**新增 Step 0.5"sub-agent 主动读取资源"
- **不**新增 Step 6.5"sub-agent 提交 skill 优化建议"
- **不**在 2 份文档任何位置出现"sub-agent 读 xiaoyingxiong references/"的字样
- **不**在 2 份文档任何位置出现"sub-agent 读 6 份深度阅读笔记"的字样
- sub-agent 工作流**仅**：
  1. 读速查表（确认分配）
  2. 读本规范（执行 + 灵活模板 + 风险）
  3. 读小说（按 SA-XX-X 指定路径）
  4. 写草稿
- 6 份笔记**不**作为 sub-agent 输入
- references/ **不**作为 sub-agent 输入

### F. skill 优化（草稿之后做，本 spec 范围外）
- 本 spec **只**做 A/B/C/D（清理 2 文档 + 扩展元素池 + 大类引导）
- skill 优化（改 SKILL.md / 新增 references/）**在草稿交付后**，用户阅读 6 笔记 + 结合草稿，再开新 spec
- 优化 skill 时**只读 SKILL.md 一个文件**（不读 references/），**可新增** references/ 文件
- **v2 SKILL 升级 8 大方向**（草稿后用户读 6 笔记时的指引方向）：
  1. 多选子类型清单
  2. XP 偏好作为子类型权重
  3. 质量权重驱动阅读与生成
  4. 分场景的多类型融合
  5. 女性向与外语的边界处理
  6. 标志场景/钩子/模板的三层分离
  7. 基于 XP 的子模板差异化生成
  8. 数据化精度的进一步细化
- **4 份可选增量 references**（草稿后用户可决定是否新增）：
  - `subtype_decision_tree.md`（必须）
  - `xp_match_patterns.md`（可选）
  - `quality_weight_rubric.md`（可选）
  - `multi_subtype_blend.md`（可选）
- 本 spec **不做** skill 任何文件改动

### G. 重新生成 12 份交付物
- 因为 2 份源文档变化（A/B/C/D），12 份交付物需重新生成
- **位置**：`/workspace/帝王战队621 1：00/documents/sub-agent-deliverables/`
- **改动点**：
  - 删除每份速查表 "## X+5" 段（已删除）
  - 删除每份速查表 "## X+1. 大文件读取提示" 段（已删除） + 重新编号 X+2/X+3/X+4 → X+1/X+2/X+3
  - 删除每份输入输出规范 "## X+1. 📖 可参考资源" 段（已删除）
  - 每份输入输出规范第三节同步扩展 13→25+ 元素 + 新增 3.4 大类引导方向（9 大子类型）
- **不**改动 12 份中的其他内容（不嵌入 Step 0.5/6.5）

## Impact（影响范围）

- **改 2 份源文档**：
  - `/workspace/帝王战队621 1：00/documents/sub-agent分配速查表.md`（806 → 742 行）
    - 删 X+1（23 行）+ 删 X+5（41 行）= -64 行
    - 重编号 X+2/X+3/X+4（标题 + 锚点）
  - `/workspace/帝王战队621 1：00/documents/sub-agent输入输出规范.md`（236 → ~310 行）
    - 删 X+1（41 行）
    - 扩展 13→25+ 元素池（+45 行）
    - 新增 3.4 大类引导方向（+45 行，9 大子类型）
- **改 12 份交付物**：每份速查表 -X 行 + 重新编号；每份输入输出规范 同步扩展
- **不动**：
  - 6 份 `pixiv_深度阅读笔记_*.md`
  - 77 部源小说（位置 `/workspace/帝王战队621 1：00/pixiv小说`）
  - 73 sub-agent 分配（`tmp/execution_packets.md`）
  - xiaoyingxiong skill（任何文件）
  - 旧 spec 4 份（已完成）
  - 3 份旧文档（`pixiv_阅读大方向.md` / `skill优化大方向.md` / `pixiv_后续任务文档.md`）

## 与之前 spec 的关系

- **v3 spec（`optimize-documents-from-notes`）方向反转**：本 spec 是 v3 的"撤销 + 重新做"版本
- **v1 spec（`split-deliverables-by-batch`）保留**：12 份交付物结构不变，但内容同步
- **v0 spec（`optimize-subagent-allocation` / `simplify-assignment-and-plan`）保留**

## 硬约束（用户要求）

1. **撤删除**：删除 2 份文档"📖 可参考资源"小节（v3 加错的，v2 不要）
2. **删 X+1**：删除速查表"X+1. 大文件读取提示"段（用户原话）
3. **扩展元素池**：13 → 25+ 元素（用户原话 + v2 SKILL 升级 8 大方向）
4. **大类引导方向**：9 大子类型 × 3-5 个方向（**不是 6 批量**，按用户原话"根据每个大类"）
5. **不嵌入 sub-agent 读 resources/ 或 6 笔记的工作流**：sub-agent 输入仅 = 速查表 + 本规范 + 小说
6. **不要写父子**：删除所有"父子"相关内容（用户原话）
7. **唯一硬性要求保持**：v2 SKILL 升级建议 ≥ 1 条
8. **三选二覆盖保持**：关键章节 / 核心套路 / 关键场景 至少 2 项
9. **500KB 硬约束保留**：在 X+3 实测边界说明中（不丢失）
10. **no 字符下限**：不锁字数
11. **方案 1 执行模式**：不换方案
12. **优化 skill 时只读 SKILL.md 一个文件**（本 spec 不做 skill 优化）
13. **可新增 references 文件**（本 spec 不做）
14. **草稿之后再读 6 笔记 + 结合优化**（本 spec 不做）
15. **原小说位置不变**：`/workspace/帝王战队621 1：00/pixiv小说`

## 不做（明确范围）

- 不改 xiaoyingxiong skill（任何文件，包括 SKILL.md 和 references/）
- 不读 6 份 `pixiv_深度阅读笔记_*.md`（草稿之后再读）
- 不读 xiaoyingxiong skill 的 references/（**只读 SKILL.md** 优化 skill，但本 spec 不做）
- 不让 sub-agent 读 references/ 或 6 笔记（v2 上一版错方向）
- 不改 77 部源小说
- 不改原小说位置（`/workspace/帝王战队621 1：00/pixiv小说`）
- 不改 73 sub-agent 数量
- 不改 `tmp/execution_packets.md`
- 不写"📖 可参考资源"小节（已删，不复活）
- 不写"必含/必标/强制减分/严禁"等硬指标
- **不写"父子"**（用户硬约束）
- **不写"父子乱伦"**（用户硬约束）
- **不写"师徒乱伦"**（用户硬约束，因为"师父-徒弟"与"父子"类似伦常关系）
- **不写"兄弟乱伦"**（用户硬约束，同理）
- 不把 13 元素灵活模板改成硬模板（仅扩展元素数，不锁元素数）
- 不在 2 份文档中复述 6 笔记的具体洞察
- 不在 2 份文档中复述 xiaoyingxiong skill references/ 的具体内容
- 不在 2 份文档中出现 6 笔记 / references/ 的路径（彻底不出现）
