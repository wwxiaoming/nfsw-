# Sub-agent 输入输出规范 - 同人+女性向（4 sub-agent）

> 生成日期：2026-06-21
> 配套：`速查表_batch-06_同人+女性向.md`（仅做分配）+ `tmp/execution_packets.md`（小说元数据）
> 批量范围：## 9. 批量 6：08同人+09女性向（4 sub-agent）


## 📥 输入清单（Input List）

**本 sub-agent 的输入由 3 部分组成**：

| 类型 | 文件 / 文件夹 | 绝对路径 | 用途 |
|---|---|---|---|
| 文档 1 | 速查表 | `/workspace/帝王战队621 1：00/documents/sub-agent-deliverables/batch-06_同人+女性向/速查表_batch-06_同人+女性向.md` | 分配指引（SA-XX-X） |
| 文档 2 | 输入输出规范 | `/workspace/帝王战队621 1：00/documents/sub-agent-deliverables/batch-06_同人+女性向/输入输出规范_batch-06_同人+女性向.md` | 执行模式 + 灵活模板 |
| 数据 | 小说文件夹 | `/workspace/帝王战队621 1：00/pixiv小说/1-6_批量分配/` | 子段已按 SA 预分配（共 73 SA） |

**sub-agent 4 步工作流**：
1. **Read** 速查表 → 找到自己的 SA-XX-X
2. **Read** 输入输出规范（同目录配套文件）→ 了解执行模式 + 灵活模板
3. **Read** 源小说（按速查表指定路径，在小说文件夹内；单段 ≤500KB 直接读，超 500KB 的子段用 `offset`/`limit` 分段）
4. **Write** 草稿（按 SA-XX-X 指定的 4.X / 5.X 段）

> ⚠ 本输入清单为 v6 优化版（2026-06-21）— 显式列出 2 份文档 + 1 个小说文件夹，让子代理一眼看清输入。

---

## 一、定位

本文件是 sub-agent 阅读小说的**输入 / 输出规范**，不约束 AI 发挥，不锁字数。

**核心原则**：
- sub-agent **按 `sub-agent分配速查表.md` 中的分配**逐本阅读
- 输出**完全自由**——AI 按作品实际内容决定详略、字数、元素组合
- 唯一硬性要求：**v2 SKILL 升级建议 ≥ 1 条**（唯一兜底质量要求）
- 大文件无需切分，按需直接 Read 全文

---

## 二、执行模式：**方案 1 — 批次内并行写草稿 → 串行合并**

> **核心思路**：同批次内 sub-agent **完全并行**（互不阻塞），各自写到独立草稿 `.md`；主代理在该批次全部完成后**串行**合并草稿到正式 6 份深度阅读笔记的 4.X/5.X 段。  
> **优势**：批次内并发度最高；草稿可独立审查、出错可重跑单本；主代理合并时按段号排序，避免写冲突。

### Step 0：主代理前置检查（每次 sub-agent 启动前必做）
1. Read 速查表（统一 1 次/批次）。
2. Read 对应批次的深度阅读笔记（确认现有 4.X/5.X 段号不冲突）。
3. 将速查表内容 + 本规范嵌入 sub-agent `message` 字段。

### Step 1-6：批次内并行写**草稿**
- 6 个批次**依次启动**（批次间串行）。
- 每批次内 sub-agent **同时启动**（最大并行）。
- 每 sub-agent 写到独立草稿文件：  
  `草稿/pixiv小说_<batch>_<SA-ID>_<作品>.md`（例：`草稿/pixiv小说_02勇者魔物奇幻_SA-02-A_豪想和你在一起.md`）
- 草稿内容**遵循 §三 灵活模板**，**不锁字数**。
- 读完即写草稿，不积压。

### Step 7：主代理**串行**合并草稿 → 正式笔记
- 主代理读取该批次所有草稿（按段号 4.1 → 4.2 → 4.3 ... 顺序）。
- 串行写入正式 6 份 `pixiv_深度阅读笔记_*.md` 的 4.X/5.X 段。
- 同一笔记文件的段号**唯一**（合并时校验）。
- 更新 TOC + 锚点。

### Step 8：阶段 4.3 验证
- 验证 6 份深度阅读笔记的实际读 X/Y (X%)。
- 验证 77 部小说全部覆盖。
- 验证 TOC 锚点有效。
- 更新 `pixiv_记忆文档.md` 第 N 次沉淀。

### 备选：方案 2（不同批次并行，同批次串行）
- 6 个批次间**并行**启动（按笔记文件分 6 个 worker）。
- 同批次内 sub-agent **串行**写入正式笔记 4.X/5.X 段。
- 流程更简单但批次内并发度低。

---

## 三、每本小说输出指引（灵活模板）

> ⚠ **本节是核心**——AI 按作品实际**自由选用 3-10 个**元素，**顺序自由**、**详略自由**、**字数不限**。

### 3.1 可选元素池（25+ 项，AI 自由选用）

| 元素 | 适用场景 | 自由发挥点 |
|---|---|---|
| **作品定位** | 几乎所有 | 核心卖点、XP 命中、目标读者 |
| **关键章节** | 几乎所有 | 选 3-10 个代表性章节，摘录关键情节 |
| **核心套路** | 几乎所有 | 列 3-10 条本小说特有套路（具体到章节号） |
| **关键场景** | 多数 | 选 3-8 个高光场景（含章节号、角色、情节） |
| **角色弧光** | 多数 | 主角 + 关键配角 + 反派的变化轨迹 |
| **世界观设定** | 奇幻/科幻/修真类 | 魔法体系/科技/修炼体系/势力分布 |
| **写作技法** | 长篇/精品 | POV 切换、节奏控制、伏笔回收、视角选择 |
| **v2 SKILL 升级建议** | **至少 1 条** | 对应 `references/` 扩展建议（如新增套路/场景/角色模板） |
| **可复用元素** | 全部 | 套路/场景/对话中可复用到其他小说的部分 |
| **戏剧冲突结构** | 长篇/精品 | 起承转合、关键转折、危机升级模式 |
| **情绪曲线** | 长篇/精品 | 主角情绪/读者情绪/关系张力的曲线分析 |
| **对白风格** | 全部 | 角色对白特点（俏皮/正式/古风/外语味） |
| **跨作品对比** | 全部 | 与同子类其他作品对比（套路异同、风格异同） |
| **9 大子类型多选清单** | 几乎所有 | 战队/勇者/正太/调教/触手/修真/同人/外语/女性向 + 主次标注（主线 1 + 副线 ≤2 + 辅线 ≤3） |
| **XP 命中标签** | 几乎所有 | 第一优先（正太/小英雄/榨精）+ 第二优先（榨精魔物/紧身衣/足控/反派主导/复仇）+ 第三优先（龟责/兽人/兄弟/双子/反英雄/穿越） |
| **质量权重（1-10 分）** | 全部 | 原始质量 + XP 调整 = 1-10 分；9-10 主模板库 / 7-8 辅助 / 5-6 借鉴 / 1-4 跳过 |
| **多子类型融合模式** | 全部 | 白曜型 / 夏日型 / 圣兽型 / 万界型 / 妨碍型（5 个经典融合）+ 冲突清单 |
| **9 大子类型建议优先级** | 全部 | 03/04/05 用户最核心；01/02 次战场；08 条件性；06 借鉴；07/09 可参考（非硬指标） |
| **标志场景三层分离** | 全部 | 第 1 层 标志场景（不可强复制）/ 第 2 层 通用钩子（可复用）/ 第 3 层 模板骨架（可复用） |
| **T1-X 子模板差异化** | 全部 | T1-榨精型 / T1-榨精魔物型 / T1-足控型 / T1-紧身衣型 / T1-正太型 |
| **数据卡 8-10 项** | 几乎所有 | 必含 5 项（身高/体重/年龄/弱点/体味）+ 建议 5 项（JJ 尺寸/足码/袜鞋品牌/战衣颜色/武器或异能） |
| **雷点命中清单** | 全部 | 雷女性 / 雷血腥 / 雷袜鞋品牌 |
| **T7-T9 新模板** | 全部 | T7 正太校园 / T8 修真玄幻 / T9 同人 IP |
| **11 份可选增量 references 建议** | 长篇/精品 | 候选见 3.5 节 |
| **子类型优先级建议（非硬指标）** | 全部 | 6 批量 → 9 子类型 + 建议权重 |
| **多子类型冲突清单（建议）** | 全部 | 可参考组合（战队+触手+足控 等），非绝对 |
| **同人原作 IP 自由度** | 同人类 | 可参考原作 IP + 自由发挥，不锁硬版权敏感度 |

### 3.2 指引原则（不锁死）

1. **AI 根据小说实际内容自由决定详略**——写得越详细越好，但**无硬性下限**。
2. **v2 SKILL 升级建议至少 1 条**（这是唯一硬性要求）——否则笔记对该 SKILL 价值不大。
3. **关键章节 / 核心套路 / 关键场景**这三项是"高价值"——AI 至少应覆盖其中 2 项。
4. **不要为了凑字数**重复铺陈；不要为了"看起来全"罗列无实质内容的章节。
5. **失败案例也要写**——若某本小说套路陈旧/情节雷同，直接在"作品定位"或"核心套路"里说明，不必硬凑元素。
6. **大文件无需切分**——单部小说（含大文件）可直接 Read 全文。

### 3.3 草稿格式建议（AI 自定）

```markdown
## 4.X 《作品名》by 作者

> 来源：/workspace/帝王战队621 1：00/pixiv小说/<子类>/<作品>/
> 实际读：X / Y 章（X%）
> 写入段：4.X

### [元素 1：作品定位]
（自由发挥）

### [元素 2：关键章节]
- **第 N 章 — 标题**：...
- **第 M 章 — 标题**：...

### [元素 3：核心套路]
- 套路 1：...
- 套路 2：...
...

### [元素 N：v2 SKILL 升级建议]
- 建议 1：references/套路/新增 xxx.md（具体到文件路径和内容大纲）
```

> AI 可以**自由增删元素**、**重命名元素标题**、**调整顺序**——本格式仅作**参考骨架**。

### 3.4 大类引导方向

> 本节是**建议方向池**——按本批量 08_同人 + 09_女性向 分组，AI 按作品实际**自由选用 N 个**作为切入点，**顺序自由**、**详略自由**、**字数不限**。每个方向是建议/可参考方向，括号内的细节是"自由发挥点"由 sub-agent 自行扩展。**不锁死方向数量**——可参考、可补充、可跨类。
>
> 本批量规范**仅列本批量**相关子类型方向（其他批量方向见各自规范，不跨列）。

#### 3.4.8 08_同人

- 关卡递进（关卡-1 → 关卡-N 递进结构）
- 神祇权威反转（神祇被反 / 反派上位）
- 玄幻血统觉醒 + 身世揭晓（血统觉醒 + 身世揭晓）
- 多英雄联动榨精（多英雄 / 多角色联动榨精）
- 原作 IP 自由发挥（不锁版权敏感度 + 自由发挥）
- 跨作品 IP 对比（漫威 / 暴雪 / 原神 / 斗罗 / 洛克人 / DC / 北欧神话 等）
- 同人钩子族（H29-H35 等同人 H 段钩子）
- 反派原型（辛达瑞拉型 / 蓝蔚羽型 / 莱恩型）
- 父子契约 → 雄堕崩塌 → 反奴神王（父子契约型反杀）
- 跨子类型融合（同人+触手 / 同人+修真 / 同人+调教 等）

#### 3.4.9 09_女性向

- 细腻情感描写（仅借鉴 09 风格）
- 群像调度（仅借鉴 09 群像）
- 反派长期榨精（仅借鉴 09 反派节奏）
- 风格借鉴（09 不进主线 / 09 不锁为模板）
- 跨子类型融合（同人+触手 / 同人+调教 + 09 风格借鉴 等）

### 3.5 11 份可选增量 references 建议

> 草稿做完后，用户会基于深度阅读笔记决定是否新增以下 references 到 xiaoyingxiong skill。本节是**候选清单**，**不**是必含。
>
> v5 增量：**删除** `sibling_dynamic_patterns.md`（兄弟/双子 互动模式），**新增** `extraction_creatures.md` + `extraction_tools.md`（v4 英文名统一）。

1. `subtype_decision_tree.md`（子类型决策表）
2. `xp_match_patterns.md`（XP 命中模式库）
3. `quality_weight_rubric.md`（质量打分标尺）
4. `multi_subtype_blend.md`（多子类型融合模式）
5. `extraction_creatures.md`（**新增**：榨精生物图鉴）
6. `extraction_tools.md`（**新增**：榨精道具库）
7. `body_data_card_extended.md`（数据卡 8-10 项扩展规范）
8. `subtype_priority_guide.md`（子类型优先级建议指南）
9. `boundary_handling_sop.md`（边界处理 SOP — 雷女性/雷血腥/雷袜鞋品牌）
10. `rhythm_subtype_specifics.md`（节奏子类型特化）
11. `vocabulary_subtype_specifics.md`（词汇子类型特化）

---

## 四、Verification Steps

### V1：实际读标注验证
```bash
grep -E "实际读 [0-9]+/[0-9]+" 草稿/*.md | wc -l
# 应 = 草稿文件数
```

### V2：写入段号验证
```bash
grep -E "^## [45]\.[0-9]+" pixiv_深度阅读笔记_*.md 草稿/*.md | wc -l
# 应 = 77
```

### V3：草稿合并验证
```bash
# 同一笔记的草稿段号不重叠
grep -hE "^## [45]\.[0-9]+" 草稿/pixiv小说_<batch>_*.md | sort | uniq -c | awk '$1 > 1 {print "DUPLICATE:", $0}'
```

---

## 五、Key Decisions（精简核心版）

| # | 决策 | 说明 |
|---|---|---|
| D1 | **无大小限制** | 大文件按需直接 Read 全文 |
| D2 | 逐本通读 | 每部 = 1 个 sub-agent，允许 ≤ 3 本合包 |
| D3 | 实时写入 | 读完一本立即写（草稿） |
| D4 | 6 批次串行启动 | 批次间串行，批次内并行（方案 1） |
| D5 | **无输出字数限制** | AI 按内容自由发挥，由 §三 灵活模板指引 |
| D6 | **无必含项硬指标** | 元素池 13 项自由选用，仅 v2 SKILL 升级建议 ≥ 1 条 |
| D7 | sub-agent 前置输入 | 速查表 + 本规范预填到 message |
| D8 | **方案 1 执行模式** | 批次内并行写草稿，主代理串行合并 |

---

## 六、风险与缓解

| 风险 | 数据支撑 | 缓解 |
|---|---|---|
| 草稿合并写冲突 | 同笔记多草稿并发 | 主代理串行合并，按段号排序 |
| 跨本合并幻觉 | 多本聚合时角色混淆 | 草稿按"每本独立 .md"组织，不混写 |
| AI 偷懒/字数过少 | 无硬性下限风险 | 灵活模板"v2 SKILL 升级建议 ≥ 1 条"作为兜底质量要求 |

---

## 七、Files to Execute

| 文件 | 操作 | 说明 |
|---|---|---|
| `工作区/tmp/execution_packets.md` | **读** | 77 部小说元数据 |
| `工作区/documents/sub-agent分配速查表.md` | **读** | sub-agent 分配指引（**仅做分配**） |
| `工作区/documents/sub-agent输入输出规范.md` | **读** | 本文件：输入输出规范 + 灵活模板 |
| `草稿/pixiv小说_<batch>_<SA-ID>_<作品>.md` | **写** | sub-agent 草稿（每本独立） |
| `工作区/pixiv_深度阅读笔记_*.md`（6 份） | **写** | 主代理合并后的正式笔记 |

---

## 八、文档关系图

```
execution_packets.md（小说元数据：77 部）
        ↓
sub-agent分配速查表.md（仅做分配，含 SA-ID / 路径 / 章节 / 段号）
        ↓
sub-agent输入输出规范.md（本文件：执行模式 + 灵活模板 + 风险）
        ↓
[执行：方案 1]
  - 批次内并行 → 草稿/*.md（每本独立）
  - 主代理串行合并 → 6 份 pixiv_深度阅读笔记_*.md
```

---

*本文件为 2026-06-21 简化版：仅保留执行模式（方案 1）和灵活模板指引，AI 完全按内容自由发挥。*

